import 'package:flutter/material.dart';

/// Central place for your brand/theme colors.
///
/// Blue = "Open"
/// Green = "Heart"
class AppColors {
  const AppColors._();

  // Brand accents
  static const Color openBlue = Color(0xFF1D4ED8);   // strong blue
  static const Color heartGreen = Color(0xFF10B981); // emerald green

  // Neutrals
  static const Color ink = Color(0xFF0B0B0B);
  static const Color paper = Color(0xFFFFFFFF);
  static const Color surfaceLight = Color(0xFFF7F7F7);
}
